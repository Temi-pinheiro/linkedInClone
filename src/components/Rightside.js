import styled from 'styled-components';

const Rightside = (props) => {
  return <Container>Right sidebar</Container>;
};

const Container = styled.div`
  grid-area: rightside;
`;

export default Rightside;
